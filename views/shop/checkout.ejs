<div class="checkout-container">
		<% if(products_in_cart){ %>
			<% products_in_cart.forEach(product=>{ %>
				<div class="checkout-items">
					<div class="product-info">
						<%= product.item.productName %> <%= product.item.price %> JPY
					</div>
						<% if(purchased_before.includes(String(product.item.id))){ %>
							<div class="purchased-before">
								<span>You already have this item</span> <a href="/shop/cart_item_delete/<%=product.item.id%>" class="btn btn-warning">
				  				(delete)
								</a>
							</div>
						<% } %>
				</div>
			<% })%>
			<div>
				Total Price <%= totalPrice %>
			</div>
		<% } %>
<form action="" method="post" id="payment-form">

	  <div class="checkout-form">
	  	<input type="text" name="first_name" class="form-control mb-3 StripeElement StripeElement--empty" placeholder="First Name">
	  	<input type="text" name="last_name" class="form-control mb-3 StripeElement StripeElement--empty" placeholder="Last Name">
	  	<input type="email" name="email" class="form-control mb-3 StripeElement StripeElement--empty" placeholder="user@example.com">
	    <div id="card-element" class="form-control">
	      <!-- A Stripe Element will be inserted here. -->
	    </div>

	    <!-- Used to display form errors. -->
	    <div id="card-errors" role="alert"></div>
	  </div>

	  <button>Submit Payment</button>
</form>
</div>
<script src="https://js.stripe.com/v3/"></script>
<script type="text/javascript" src="/js/checkout.js"></script>
<!-- <link rel="stylesheet" href="/css/style.css"> -->
